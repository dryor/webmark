{
  "name": "@webmarkjs",
  "version": "0.0.1",
  "license": "MIT",
  "private": true,
  "packageManager": "pnpm@10.14.0",
  "scripts": {
    "build": "pnpm -r --filter \"@webmarkjs/*\" build",
    "test": "pnpm -r --filter \"@webmarkjs/*\" test",
    "check": "pnpm -r --filter \"@webmarkjs/*\" check",
    "version": "changeset version",
    "prepare": "git config core.hooksPath .githooks"
  },
  "devDependencies": {
    "@changesets/cli": "^2.31.0"
  }
}